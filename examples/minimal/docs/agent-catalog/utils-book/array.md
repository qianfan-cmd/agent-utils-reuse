# Utils 工具书 — 第 array 章 — src/utils/array/

> **DO NOT EDIT** — 由 `pnpm gen:utils-book` 生成。

摘要仅供 **Shortlist**；**reuse** 须在 Read 源码并完成 **Confirm（五问）** 之后。

### `array/sortArray.ts`

**路径**: `src/utils/array/sortArray.ts`

**用途**: 数组排序工具

| 符号 | 摘要 | 行号 |
|------|------|------|
| `sortAsc` | 数字数组升序排序，返回新数组，不修改原数组 | L6 |
| `sortDesc` | 数字数组降序排序，返回新数组 | L11 |

### `array/uniqueByKey.ts`

**路径**: `src/utils/array/uniqueByKey.ts`

**用途**: 数组去重工具

| 符号 | 摘要 | 行号 |
|------|------|------|
| `uniqueByKey` | 按字段对对象数组去重，保留首次出现的元素 | L6 |
