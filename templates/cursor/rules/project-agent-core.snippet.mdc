---
description: Optional overlay — merge utils Confirm gate into your project agent-core rule
alwaysApply: false
---

# Project agent-core overlay (optional)

Copy relevant bullets into your existing `ai-web-agent-core.mdc` or project equivalent:

## Utils Confirm gate (add under 动手前)

4. If Implement touches `@/utils` or shared util logic: first assistant message must include **Confirm (Q1-Q5) + Verdict（最终）** in chat; Hook `hookMode: confirm` denies Write until util **source** was Read this session.

## Do not

- Skip Confirm because WIP already imports utils
- Write `.utils-discovery-cache.json`

See `.cursor/rules/utils-reuse-gate.mdc` and `AGENTS.md` utils reuse section.
